<%- model_class = ExpenseType -%>
<div class="box span12">
  <div class="box-header">
    <h2>
      <i class="icon-align-justify"></i><span class="break"></span>
      <%=t '.title', :default => model_class.model_name.human.pluralize %>
    </h2>
    <div class="box-icon">
      <a href="#" class="btn-minimize"><i class="icon-chevron-up"></i></a>
    </div>
  </div>
  <div class="box-content">
    <table class="table table-striped">
      <thead>
        <tr>
          <th><%= model_class.human_attribute_name(:id) %></th>
          <th><%= model_class.human_attribute_name(:name) %></th>
          <th><%= model_class.human_attribute_name(:description) %></th>
          <th><%= model_class.human_attribute_name(:townhouse_id) %></th>
          <th><%= model_class.human_attribute_name(:created_at) %></th>
          <th><%=t 'helpers.actions' %></th>
        </tr>
      </thead>
      <tbody>
        <% @expense_types.each do |expense_type| %>
          <tr>
            <td><%= link_to expense_type.id, syndic_townhouse_expense_type_path(@townhouse, expense_type) %></td>
            <td><%= expense_type.name %></td>
            <td><%= expense_type.description %></td>
            <td><%= expense_type.townhouse_id %></td>
            <td><%=l expense_type.created_at %></td>
            <td>
              <%= link_to t('helpers.links.show'), syndic_townhouse_expense_type_path(@townhouse, expense_type), :class => 'btn btn-mini btn-info' %>
              <%= link_to t('helpers.links.edit'), edit_syndic_townhouse_expense_type_path(@townhouse, expense_type), :class => 'btn btn-mini' %>
              <%= link_to t('helpers.links.destroy'), syndic_townhouse_expense_type_path(@townhouse, expense_type),
                          :method => :delete,
                          :data => { :confirm => t('helpers.links.confirm') },
                          :class => 'btn btn-mini btn-danger' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= link_to t('helpers.links.new'), new_syndic_townhouse_expense_type_path(@townhouse), :class => 'btn btn-primary' %>
  </div>
</div>